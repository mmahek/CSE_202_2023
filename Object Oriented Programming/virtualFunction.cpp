#include<iostream>
using namespace std;

//a virtual function is a member function in the base class that we expect to redefine in derived class.
// a virtual is used in base class in order to ensure that the functionis overriden. This is especially applied to class where a pointer to an object of a derived class.
int main(){


    return 0;
}